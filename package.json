{
  "name": "cli-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist && mkdir dist",
    "rollup": "rollup src/app.js -o dist/app.js -f iife -m",
    "uglify": "uglifyjs dist/app.js -o dist/app.js -c -m --source-map content='dist/app.js.map',url='app.js.map'",
    "build": "npm run clean && npm run rollup && npm run uglify"
  },
  "author": "Dustin Dowell <dustindowell2@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "rollup": "^0.57.1",
    "uglify-js": "^3.3.16"
  }
}
